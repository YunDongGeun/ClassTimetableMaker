<Page x:Class="ClassTimetableMaker.Views.DynamicPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ClassTimetableMaker.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Background="White"
      Title="DynamicPage">

    <!--<DockPanel>
        --><!-- 컨트롤 패널 --><!--
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="10">
            <Button Name="AddElementButton" Content="요소 추가 (크기 증가)" 
                    Click="AddElement_Click" Margin="5"/>
            <Button Name="RemoveElementButton" Content="요소 제거 (크기 감소)" 
                    Click="RemoveElement_Click" Margin="5"/>
            <TextBlock Text="현재 크기: " VerticalAlignment="Center" Margin="10,0,5,0"/>
            <TextBlock Name="SizeLabel" Text="4x4" VerticalAlignment="Center" FontWeight="Bold"/>
        </StackPanel>

        --><!-- 동적 그리드 영역 --><!--
        <Border DockPanel.Dock="Top" BorderBrush="Gray" BorderThickness="2" Margin="10">
            <Grid Name="DynamicGrid" Background="WhiteSmoke">
                --><!-- 동적으로 생성될 그리드 --><!--
            </Grid>
        </Border>
    </DockPanel>-->
</Page>
